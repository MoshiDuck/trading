enum TypeTransaction {
  PAIEMENT,
  FACTURE,
  ECHANGE,
}

enum TypeTrade {
  ACHAT,
  VENTE
}

enum TypeVente {
  TAKE_PROFIT,
  STOP_LOSS,
  TRAILING_STOP,
  VOLATILITE_ELEVEE,
  AUCUNE, VENTE_FORCEE,
}

enum ProfitType {
  GAIN,
  PERTE
}